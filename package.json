{
  "name": "grpc-fakeverse",
  "version": "1.0.0",
  "description": "Fake data gRPC server — 4 services, 40 RPCs, gRPC reflection enabled.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@faker-js/faker": "^9.0.0",
    "@grpc/grpc-js": "^1.12.0",
    "@grpc/proto-loader": "^0.7.0",
    "@grpc/reflection": "^1.0.4"
  }
}
